# In Python, a tuple is an ordered, immutable collection of items. Once a tuple is created, its elements cannot be changed, added, or removed.




# When should you use a tuple?

# The data should not change (e.g., days of the week, RGB color values).

# You want a lightweight, read-only collection.

# You need a hashable sequence to use as a dictionary key or set element.